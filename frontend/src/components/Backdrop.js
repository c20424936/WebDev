import React from 'react';

const Back = (props) => {
  return (
    <div className="Back" onClick={props.onClick}/>
    
  ) /*The Black Backdrop when clicking on the moreInfo button on the site */
}

export default Back